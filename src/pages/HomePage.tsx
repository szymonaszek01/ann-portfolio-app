const HomePage = () => {
  return (
    <div
      className={`w-full h-full min-h-[100vh] flex items-center justify-center text-center`}>
      <p className={`text-yellow-gradient font-semibold text-xl`}>Welcome in Ann's portfolio page!</p>
    </div>
  );
};

export default HomePage;
